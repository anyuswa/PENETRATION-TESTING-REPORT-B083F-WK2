# Evidence

This directory contains the eight embedded images extracted from the submitted report.

- `image1.png` through `image8.png`

The original report states that screenshots were collected as evidence for the practical activities. The source document does not provide a reliable machine-readable caption-to-image mapping for every image, so filenames are preserved rather than guessing which screenshot belongs to which step.
